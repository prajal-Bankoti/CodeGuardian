# Bitbucket OAuth Configuration
# Copy this file to .env.local and replace with your actual credentials
# Get these from: https://bitbucket.org/account/settings/app-passwords/

REACT_APP_BITBUCKET_CLIENT_ID=your-client-id-here
REACT_APP_BITBUCKET_CLIENT_SECRET=your-client-secret-here
REACT_APP_BITBUCKET_REDIRECT_URI=http://localhost:3000/app/bitbucket-login

# Instructions:
# 1. Copy this file: cp env.example .env.local
# 2. Go to https://bitbucket.org/account/settings/
# 3. Navigate to "OAuth consumers" in the left sidebar
# 4. Click "Add consumer"
# 5. Fill in:
#    - Name: CodeGuardian
#    - Callback URL: http://localhost:3000/app/bitbucket-login
#    - Scopes: Repositories (read), Pull requests (read), Account (read)
# 6. Copy the Key and Secret from your OAuth app
# 7. Replace the values in .env.local with your actual credentials
# 8. Restart the development server: yarn dev